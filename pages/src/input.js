import Layout from '../components/layout';

export default function Input () {
  //add loading

  //button untuk keluar jika tidak ada data yang ingin diinput lagi
    return(
        <Layout>
        <p>Membaca input beras disini
        </p>
        <a href="/app/home"><button style={{}} type="button">Keluar</button></a>
        </Layout>
    );
}